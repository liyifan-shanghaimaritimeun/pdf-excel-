# 更新版本（表格检测程序包）
- `更新版本.exe`：主程序（489MB，经 Git LFS 管理）
- `models/`：whl 模型包（cls / det / formula / layout / rec / table）
- `weights/`：`table_1cls_dev.pt`
- `model_db/`：`electronic.jsonl` / `mechanical.jsonl`
